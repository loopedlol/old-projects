# Age API Project

## Description
A small web page that guesses an age from a typed name using the agify.io API.

## Original Context
This was an older learning project.

## How to Run
Open `index.html` in a browser.

## Status
Works

## Cleanup Notes
- Renamed folder from `APIProject` to `age-api-project`.
- Encoded the submitted name before calling the API.
- Added a basic message for API/network failures.
- Added `.gitignore`.
