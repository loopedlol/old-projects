# CSharp Area Practice

## Description
A C# console practice project that calculates the area of a rectangle and doubles its height.

## Original Context
This was an older learning project.

## How to Run
Run from the project folder:

```sh
dotnet run --project idktoName/idktoName.csproj
```

## Status
Partially works

## Cleanup Notes
- Renamed folder from `C#practice` to `csharp-area-practice`.
- Moved generated `bin/`, `obj/`, and editor settings into `_excluded_review/`.
- Added a minimal `Main` method so the console project has a runnable entry point.
- Removed an unused import.
- Added `.gitignore`.
