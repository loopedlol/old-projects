# Flask Auth DB Experiment

## Description
An incomplete Flask authentication and database experiment.

## Original Context
This was an older learning project.

## How to Run
Archived. The preserved cleaned stub can be run with:

```sh
python3 -m pip install -r requirements.txt
python3 app.py
```

## Status
Archived

## Cleanup Notes
- Renamed folder from `backendsec` to `flask-auth-db-experiment`.
- Moved notes into `_excluded_review/`.
- Moved the original `app.py` with a hardcoded secret and incomplete database code into `_excluded_review/security_review/`.
- Added a small cleaned Flask stub that renders the existing `home.html`.
- Added `requirements.txt` and `.gitignore`.
