# Flask Forum

## Description
A simple Flask forum/blog practice app with topic pages and a basic post form.

## Original Context
This was an older learning project.

## How to Run
Run from the project folder:

```sh
python3 -m pip install -r requirements.txt
python3 app.py
```

Open `http://localhost:5555/`.

## Status
Partially works

## Cleanup Notes
- Renamed folder from `Forum` to `flask-forum`.
- Moved screenshot and notes into `_excluded_review/`.
- Added missing topic templates for existing routes and nav links.
- Added a small client-side `send()` function for the existing post buttons.
- Added `requirements.txt` and `.gitignore`.
