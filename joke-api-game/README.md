# Joke API Game

## Description
A small web page that gets jokes and punchlines from an external joke API.

## Original Context
This was an older learning project.

## How to Run
Open `index.html` in a browser.

## Status
Works

## Cleanup Notes
- Renamed folder from `JokeGame` to `joke-api-game`.
- Encoded the joke type before calling the API.
- Added basic checks for missing jokes, network failures, and clicking punchline before loading a joke.
- Added `.gitignore`.
