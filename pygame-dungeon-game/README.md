# Pygame Dungeon Game

## Description
A Pygame dungeon practice project with generated levels, player movement, doors, and a simple moving enemy.

## Original Context
This was an older learning project.

## How to Run
Run from the project folder:

```sh
python3 -m pip install -r requirements.txt
python3 main.py
```

## Status
Partially works

## Cleanup Notes
- Renamed folder from `PygameGame` to `pygame-dungeon-game`.
- Moved virtual environment and Python cache files into `_excluded_review/`.
- Fixed window quit handling.
- Added `requirements.txt` and `.gitignore`.
